//this is where I create a variable to hold my new object

Object_Name object_instance;



void setup(){
  //this is where I construct and initialize my object
  object_instance = new Object_Name(SET VARIABLES HERE);
}

void draw(){
  //This is where I can interact with the object and use its methods.
  object_instance.var1 = 100;
  
  object_instance.Method_Name();
}



class [ObjectName]{

  //this is where the variables get declared that are properties of ObjectName
  //for example: 

  float var1;
  boolean var2;

  //this is where i build the constructor
  Object_Name(float tempInput){

  //this is where i use the temp values to set
  //the initial value of my parameters (var1, var2, etc)
  }

  //This is where I define the methods for my object.
  void Method2_Name(float tempInput){

  return(var_to_return){

  }
}


--------------------------- EXAMPLE -----------------------------
Ball ball1;

void setup(){
ball1 = new Ball(100,100,20,color(100,100,250));
}

void draw(){
  ball1.display();
}

class Ball(){
  float xpos;
  float ypos;
  float size;
  color c1;


  Ball(float tempx, float tempy, tempSize, tempC){
    xpos = tempx;
    ypos = tempy;
    size = tempsize;
    c1 = tempC;
    }
  }
  
  void display(){
  fill(c1);
  ellipse(xpos,ypos,size,size);
  
